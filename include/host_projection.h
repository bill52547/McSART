#ifndef _HOST_PROJECTION_H
#define _HOST_PROJECTION_H
#include "universal.h"
#include "kernel_projection.h" // kernel_projection(d_proj, d_img, angle, SO, SD, da, na, ai, db, nb, bi, nx, ny, nz);
#endif // _HOST_PROJECTION_H
